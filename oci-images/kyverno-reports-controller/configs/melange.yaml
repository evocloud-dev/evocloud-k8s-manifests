# SPDX-License-Identifier: Apache-2.0
# Source: https://github.com/kyverno/kyverno
# Schema: https://github.com/chainguard-dev/melange/blob/main/docs/BUILD-FILE.md
# Reference: https://github.com/kyverno/kyverno/blob/main/Makefile

package:
  name: kyverno-reports-controller
  version: 1.16.0
  epoch: 1
  description: "Kyverno Reports Controller"
  target-architecture:
    - x86_64
  copyright:
    - attestation: |
        Created by the EvoCloud Engineering Team.
        Copyright (C) 2025 EvoCloud, Inc.
      license: Apache-2.0
  url: "https://github.com/kyverno/kyverno"

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - ca-certificates-bundle

pipeline:
  - name: "Prepare extraction directory"
    runs: |
      mkdir -p reports-controller

  - name: "Download Kyverno source"
    uses: fetch
    with:
      uri: https://github.com/kyverno/kyverno/archive/refs/tags/v${{package.version}}.tar.gz
      expected-sha512: 2c57c73de8b85c3ec80fd8568b83250214069489b7dc7437418f39c29866dd5e9c928ff2be15b65fd4f0f00b8002e9288946dfee9efbb5f62cdc97adc0de8042
      directory: reports-controller
      delete: true

  - name: "Build Reports Controller"
    uses: go/build
    with:
      modroot: reports-controller
      packages: ./cmd/reports-controller
      # SPDX-License-Identifier: Apache-2.0
# Source: https://github.com/kyverno/kyverno
# Schema: https://github.com/chainguard-dev/melange/blob/main/docs/BUILD-FILE.md
# Reference: https://github.com/kyverno/kyverno/blob/main/Makefile

package:
  name: kyverno-reports-controller
  version: 1.16.0
  epoch: 0
  description: "Kyverno Reports Controller"
  target-architecture:
    - x86_64
  copyright:
    - attestation: |
        Created by the EvoCloud Engineering Team.
        Copyright (C) 2025 EvoCloud, Inc.
      license: Apache-2.0
  url: "https://github.com/kyverno/kyverno"

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - ca-certificates-bundle

pipeline:
  - name: "Prepare extraction directory"
    runs: |
      mkdir -p reports-controller

  - name: "Download Kyverno source"
    uses: fetch
    with:
      uri: https://github.com/kyverno/kyverno/archive/refs/tags/v${{package.version}}.tar.gz
      expected-sha512: 2c57c73de8b85c3ec80fd8568b83250214069489b7dc7437418f39c29866dd5e9c928ff2be15b65fd4f0f00b8002e9288946dfee9efbb5f62cdc97adc0de8042
      directory: reports-controller
      delete: true

  - name: "Build Reports Controller"
    uses: go/build
    with:
      modroot: reports-controller
      packages: ./cmd/reports-controller
      # secfixes:
      #   1.16.0-r1:
      #     - GHSA-j5w8-q4qc-rx2x
      #     - GHSA-f6x5-jh6r-wrfv
      deps: golang.org/x/crypto@v0.45.0
      ldflags: "-s -X github.com/kyverno/kyverno/pkg/version.BuildVersion=${{package.version}}"
      output: kyverno-reports-controller

  - uses: strip

  - name: "Cleanup"
    runs: |
      rm -rf reports-controller

      ldflags: "-s -X github.com/kyverno/kyverno/pkg/version.BuildVersion=${{package.version}}"
      output: kyverno-reports-controller

  - uses: strip

  - name: "Cleanup"
    runs: |
      rm -rf reports-controller

